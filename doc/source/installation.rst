============
Installation
============

At the command line::

    $ pip install keystone-playground

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv keystone-playground
    $ pip install keystone-playground
